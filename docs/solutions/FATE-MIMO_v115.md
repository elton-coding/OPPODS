# FATE-MIMO：DataFountain 1176 获奖技术方案（冻结版 V115）

## 1. 最终结论

最终提交是 **FATE-MIMO（Fairness-Aware Task-oriented End-to-End MU-MIMO）**。方案由五部分组成：

1. UE 将完整 CSI 解析压缩为任务等效信道，在角度－时延域固定保留 6 个主要时延模态，恰好映射到 96 个复数上行 RE。
2. BS 使用带解析 Wiener 基线的 SNR 条件化残差 Transformer 去噪，再用鲁棒 RZF 完成两用户预编码。
3. 下行使用分层 Gray 256-QAM；每 12 个子载波保留 1 个 Walsh 导频，其余 11 个承载数据，每 UE 主载荷为 1056 bit。
4. 导频相位在足够高的 SNR 下自适应携带 2～5 个额外 bit。
5. 5-bit 控制信道同时完成用户导频身份协调和约束尾部向量量化，把“等价的 SNR 分隔阈值”转化为额外通信能力。

严格复刻官方逐样本接口的本地评测结果如下（2000 个信道样本、4000 个 UE 分数、seed 1176）：

| 指标 | 冻结结果 |
| --- | ---: |
| 效率均值 | 68.133659 |
| 公平分 P10 | 49.956597 |
| 加权总分 | **62.680540** |
| 最短 / 最长输出 | 1 / 1152 bit |
| 完整 1152-bit 输出 | 2065 / 4000 UE |
| 上一稳定版 v62 | 62.454635 |
| 绝对提升 | **+0.225905** |

以上为确定性本地验证结果，不冒充线上排行榜成绩。

## 2. 与官方评分公式对齐

单 UE 得分为：

\[
\mu=50+\frac{100}{1152}\sum_{k=1}^{B}
\left(\mathbf 1[\hat b_k=b_k]-0.5\right).
\]

未输出 bit 自动按 50% 正确率计分。因此：

- 只有预测正确率高于 50% 的 bit 才值得输出；
- 目标是累计“超出随机猜测”的正确 bit 数，而不是传统整块 BLER；
- 极低 SNR 输出过多随机 bit 会扩大分数方差并伤害 P10；
- 少量无噪控制 bit 最适合传递长尾的统计量或码字索引，而不是逐 bit 生搬硬套。

最终版在 `SNR < -15.5 dB` 时仍只输出 1 bit，把公平分稳定在约 49.9566。中高 SNR 才使用尾部模板补全，因此效率提升没有交换掉公平性。

## 3. 任务导向稀疏反馈

训练信道具有很强的频率和时延结构。UE 先对每个 12 子载波组的 `2×2` 接收协方差取主特征向量，形成规范相位的接收合并器，再得到每组 16 维任务等效信道。

对 12 个频率组做时延变换、对 16 根发射天线做角度变换，固定发送模态 `[0,1,2,3,11,10]`。于是反馈维度为 `6×16=96` 个复数符号，无需额外发送稀疏索引。固定索引比低上行 SNR 下传离散位置更稳健。

## 4. 解析 Wiener + 残差 Transformer

BS 去噪器先根据时延模态能量先验和 `SNR_UL=SNR_DL-10 dB` 计算解析 Wiener 估计。Transformer 同时接收：

- 含噪的 6×16 复数系数；
- Wiener 估计；
- DL SNR 与等效 UL 噪声特征。

网络只预测解析解的残差。最终冻结检查点为：

```text
checkpoints/sparse_denoiser_task_reg000439453125_rec08_ft1e5.pt
```

关键任务微调参数为：正则 `0.00439453125`、重构权重 `0.8`、学习率 `1e-5`、2000 step。该结构兼顾可解释性、低参数量和信道分布外推。

## 5. 鲁棒预编码与保留导频接收

每个频率组采用两用户鲁棒 RZF：

\[
W_g=\hat G_g^H(\hat G_g\hat G_g^H+\alpha\bar\sigma^2I)^{-1}.
\]

冻结版保留导频 profile 的正则系数为 `0.45`。每 12 个子载波的 offset 5 放置导频；两个用户使用常量 / 交替 Walsh 码分离，导频幅度为 `1.5`。其余 11 个子载波发送分层 Gray 256-QAM，共 1056 bit。

Receiver 用本地真实信道构造参考波束，对两个 Walsh 导频假设计算相似度并确定自己的导频角色；随后使用加载协方差空间滤波、导频增益迭代、数据向量细化、局部数据增益细化和软干扰抵消。冻结参数见 `configs/final.yaml`。

## 6. 核心创新：控制阈值约束尾部向量量化

### 6.1 两用户都处于高 SNR

当 `min(SNR_0,SNR_1) >= 10.25 dB` 时，本地 Walsh 身份识别已足够可靠，不再需要控制字传 SNR 阈值。控制值 `0～4` 从 5 个固定联合尾部模板中选择一个，压缩两名用户尚未发送的 bit 尾部。接收端根据自己的高/低角色恢复对应模板，最终可输出完整 1152 bit。

### 6.2 SNR 不对称时的“等价阈值”

其余场景使用控制值 `5～31`，对应 27 个阈值。阈值覆盖 `[-20,10.25] dB`，间隔约 1.12 dB。

传统做法只取低用户 SNR 上方最近的量化阈值。但只要阈值满足：

\[
SNR_{low}\le T < SNR_{high},
\]

两名接收端的角色判定都完全相同。因此多个控制值在“身份协调”任务上是等价的。发送端遍历所有合法阈值，并选择使高 SNR 用户剩余尾部与固定模板海明距离最小的控制值。接收端一方面用该阈值判断角色，另一方面用同一个控制索引恢复尾部模板。冻结版把相邻阈值模板组织成互补对；当可行阈值只有 2～4 个时，“取最大相关”比相互独立模板具有更高的期望增益。

这一机制不改变官方接口、不增加控制 bit，也不依赖接收端知道 UE ID。它把控制量化的冗余变成了一个带可行域约束的向量量化问题。

### 6.3 分段公平保护

- `SNR < -15.5 dB`：只输出 1 bit，保护 P10；
- `-15.5 <= SNR < -9.5 dB`：普通低角色输出 924-bit 前缀；明确的高角色可用最长 228-bit 模板补全；
- `SNR >= -9.5 dB`：解调 1056-bit 主载荷和自适应相位 bit，高角色再用阈值模板补全；
- 双高 SNR：使用 5 码字联合尾部 VQ。

## 7. 严格消融与晋级纪律

所有候选先经过 500 样本筛选，只有初筛超过基线才运行固定 seed 的 2000 样本官方同口径复验。只有严格净增益版本才替换生产目录。

| 版本 | 严格总分 | 结论 |
| --- | ---: | --- |
| v62：原稳定版 | 62.454635 | 基线 |
| v102：8 尾部码字 | 62.476070 | 提升 |
| v104：7 尾部码字 | 62.481128 | 提升 |
| v107：5 尾部码字 | 62.482602 | 提升 |
| v111：平衡五码本 | 62.481949 | 严格回退 |
| v112：仅高 SNR 阈值 VQ | 62.566623 | 大幅提升 |
| v113：主解调区阈值 VQ | 62.668357 | 大幅提升 |
| v114：中间前缀阈值 VQ | 62.679188 | 提升 |
| **v115：互补阈值模板** | **62.680540** | **冻结最佳** |

独立 seed 1177 的同口径 2000 样本交叉复验中，v114 为 `62.886006`，v115 为 `62.894771`，互补模板继续提升 `+0.008765`，说明该改进不是 seed 1176 的偶然波动。最终模型在 seed 1178 上得到 `62.907000`，P10 仍为 `49.956597`。

被拒绝的路线还包括：控制位过度复用导致排序量化过粗、相位门限偏移、幅度调制、残差缩放、多个独立微调种子、检查点 soup、过强数据增益/向量细化、联合检测和相位斜率编码。失败候选均保留在 `artifacts/candidate_*`，没有覆盖稳定包。

## 8. 复现、测试与提交

环境：

```text
D:\Tools\Anaconda\envs\oppods-df1176
Python 3.11.15
PyTorch 2.10.0+cu128
NVIDIA GeForce RTX 5090 D
```

复现命令：

```powershell
$python = 'D:\Tools\Anaconda\envs\oppods-df1176\python.exe'
& $python -m pytest -q
& $python scripts/evaluate_submission.py --submission modelSubmit --samples 2000 --seed 1176 --device cuda
```

最终文件：

- `modelSubmit/modelDesign.py`：自包含官方接口实现；
- `modelSubmit/encoder.pth`、`transmitter.pth`、`receiver.pth`：冻结权重；
- `artifacts/FATE_MIMO_submission.zip`：上传别名；
- `artifacts/FATE_MIMO_submission_threshold_vq_v115_official.zip`：符合比赛示例层级的不可变上传包；
- `artifacts/FATE_MIMO_submission_threshold_vq_v115.zip`：用于本地平铺评测的等价包；
- `artifacts/final_results.json`：机器可读结果；
- `configs/final.yaml`：冻结参数。

官方层级冻结包大小为 2,266,343 字节，SHA-256：

```text
D4A045E81E360C927E67EA9522E01A8A5556B59AD68F3090BD669C728592062C
```

ZIP 严格采用比赛原文示例：`submit_pt/modelDesign.py` 与 `submit_pt/modelSubmit/{encoder,transmitter,receiver}.pth`。32 项测试、静态检查和 ZIP CRC 校验均通过；其中 3 项提交专用回归测试固定了互补模板、控制索引范围和官方 ZIP 成员。
