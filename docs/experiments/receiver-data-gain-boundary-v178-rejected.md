# V178 Receiver 数据增益启用边界细扫（拒绝）

日期：2026-08-25  
分支：`codex/receiver-data-gain-boundary-v178`  
对照：V156，`DATA_GAIN_REFINEMENT_MIN_SNR_DB=-5.0`

## 结果

在 seed1176、500 样本官方兼容 exact 评测上，直接移动数据增益细化的启用
边界。每个候选天然只改变紧邻 `-5 dB` 的 0.25–0.5 dB 区间，因此不再
从结果中二次选择 SNR 边界。V156 final 为 `62.844883`。

| 候选边界 | 改变的 SNR 区间 | final | 相对 V156 | P10 变化 |
|---:|---|---:|---:|---:|
| `-5.50` | 在 `[-5.5,-5)` 新增细化 | 62.843911 | -0.000972 | 0 |
| `-5.25` | 在 `[-5.25,-5)` 新增细化 | 62.844457 | -0.000425 | 0 |
| `-4.75` | 在 `[-5,-4.75)` 关闭细化 | 62.844397 | -0.000486 | 0 |
| `-4.50` | 在 `[-5,-4.5)` 关闭细化 | 62.844154 | -0.000729 | 0 |

## 结论

四个候选在第一个发现 seed 上全部为负，违反逐 seed 非退化门禁；不扩样、
不进入盲测、不修改模型。保留 V156 的 `-5.0 dB` 边界。
