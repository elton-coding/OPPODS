# V258：提前登记的共享窗口条件性确认

## 2026-09-10 22:24：实现与冻结计划完成，尚未启动等待任务

特性分支 `codex/frozen-confirmation-v258`。V258已完成72k训练和固定选型审计，本地均分68.65581090856482；对同预算V250净增0.04978237682978204，95%区间[0.005641295173957299,0.07876507569869133]，三组总分均升。对V257仅增0.005212652047471049，区间跨0，不能以最高点估计直接认定优于V257。

本次只补足已完成候选的确认，不新增训练因素、预算或模型，不触碰正在训练或排队的V259—V262代码和来源。

## 预先固定的协议

固定split1176、test offset8000、2000信道、noise52701/52702/52703、原逐样本GPU评测、完整1152位。候选标签v258_confirm，固定四文件来自artifacts/pure_neural_v258/eight/endtoend_steps72000，不重选检查点，不追加训练。

两项参照分别为V250（v250_confirm_v257）和V257（v257_confirm）。在两者的确认结果均未产生前登记，待V257原确认完整结束后，只读复用其六份原始缓存及两份完整报告，不重复推理参照模型，不修改V257的111项冻结计划或决定。

每一比较均要求三噪声配对总分增量全部>0，且按信道聚类、两UE一起、跨噪声共享索引的bootstrap2000次（seed227）95%区间下界>0。分别报告对V250的改进证据，以及是否同时优于V250和V257；只有两比较均通过才支持优先于这两个冻结参照。P10与效率变化单独披露。对届时其他冠军仍需检查，脚本不自动晋级或提交赛站。

不能在看到确认结果后更改窗口、种子、模型、比较对象或门槛。登记前后均拒绝任何V257确认输出或失败记录已存在的情况；实际登记时上述结果均不存在，V257原等待器仍存活且未开始推理。

## 来源与统计防护

V258完整73点训练报告、四文件、原73项输入和9项完成依赖均核对；重新从固定offset2000缓存复现其原始选型统计。V250/V257/V258九份来源缓存同时检查真实split1176 offset2000 ID、两UE配对、三噪声顺序、SNR一致和全1152位。

确认阶段检查真实offset8000的4000行ID，而非只相信标签或元数据；缓存必须有限、分数0—100、SNR[-20,20]、每UE1152位。完整报告与冻结模型、三组原始指标及缓存统计对应。原始float64统计与float32缓存只允许1e-4舍入差异；原始均分须精确复现三组报告均值。

V257确认完成后另建不可覆盖的v258_confirmation_reference.json，冻结其两报告、六缓存和决定。V258推理前后核验这些文件以及已登记136输入；两配对统计从原始缓存重算。计划、输出、锁、失败记录均排他创建，存在部分输出时禁止隐式续跑或覆盖。

## 数据限制

沿用已登记的V257窗口清查：与当前split1176训练/验证/选型窗口无交叉，但1812/2000信道曾出现在更早的其他划分评测；另有无ID记录和祖先训练来源缺口。它是当前候选组的条件性确认，不是全项目盲测，不保证完全无历史泄漏。

本确认与V257确认共享数据和参照，不能称为两次相互独立的验证。bootstrap区间仅描述有限信道的不确定性，不消除历史反复选型或线上分布偏移。

## 实现与登记证据

脚本scripts/run_frozen_confirmation_v258.py，SHA63d72ea0a99a36849b3fd8964336dbf7ffa6ef51504c0b5d0222fe587aae4733。42项专项包含错误窗口（所有组一致错位也拒绝）、配对与位数、非法统计、双参照门禁、登记竞态、已有部分输出及来源变化。Ruff通过；完整CPU回归会话61137已exit0，497通过10跳过，42.67秒，无GPU训练。

注册会话68747已exit0，生成benchmarks/v258_confirmation_plan.json，冻结136项，SHA18f0e4f5591156c0c5d35905e45061054b4523ccae1804dc47f8fec1cdfa6bbd；evaluation_started=false。先推送本次实现、计划与说明，再启动run_frozen_confirmation_v258.py --wait-timeout86400。等待V257完整确认再申请GPU槽，与已登记队列共存，最多两项GPU重任务。不购买算力，不自动上传。

此刻尚无等待器、确认推理、确认分数或新发布包。主干V250不变，69目标未达到。后续启动状态以此文顶部和总交接最新记录为准。
