# V259：八专家RMS损失的全局batch400，匹配720万信道抽样

2026-09-10预登记；分支codex/pure-neural-rms-global-batch-v259从2e5d5b7创建。当前仅准备CPU证明及独立排队代码，V256/V257继续训练，V258继续等待；不新开第三项GPU任务。主干V250本地68.60602864583332、线上未知，69未达。

## 唯一因素与预算

V246/V247只覆盖两专家、未归一化soft_score、120万抽样的大批次条件，未证明净收益；尚未覆盖当前八专家RMS/720万抽样。V259检验扩大参与一次评分代理的UE集合是否改善当前模型。每次400信道/800UE一起算全局RMS评分损失，不能把四个batch100的P10分别算后平均。

相对V250，只改变训练批次分组100→400；总抽样保持720万，所以更新次数72000→18000、验证间隔1000→250，均每10万训练信道抽样验证一次，共73个验证点。统一Adam1e-5不随batch缩放，patience72不变。这一因素必然同时改变每次更新样本数和总更新次数，不能只归因为P10方差；相同seed也不保证分组后的训练抽样、bits/SNR、噪声流等同。720万是抽样次数，不是720万个独立信道，也不是等墙钟算力比较。

架构仍原V230八个5dB联合min-SNR专家、512×10、Encoder冻结、Tx/Rx训练、1152位/k8；从原V227映射[0,0,1,1,1,1,1,1]fresh Adam独立开始。RMS T0.5/公平0.3/带宽0.025/原始BCE0.05、全SNR均匀、seed15240、FP32/cap0.4、验证2000且固定原batch100均不变。不与温度、共享前缀、解冻Encoder或余弦组合。

## 全局损失与运行证明

使用已验证的V246非重入激活重算与显式逐microbatch噪声生成器重放；microbatch100仅控制激活保存，不改变全局400信道一次loss、一次裁剪和一次Adam。新增隔离入口组合现有RMS损失和全局前向，不修改任何在训源码；退出或异常恢复进程内替换。

CPU真实父权重16个训练信道/八路由、micro8，比较无重算与重算：全局logits/loss相同，全部Tx/Rx梯度在atol1e-7/rtol1e-5内相同，反向不改变后续显式随机流；Encoder保持冻结/权重不变，两次真实Adam状态完整有限。此证明只对应相同分块链路，不证明与未分块batch400或主干batch100的训练随机流相同，不是成绩。

排队预定等待V257完整72k固定审计后释放槽；前驱仅是资源依赖，完整来源另冻结。若前驱或注册对照达到本地69，先暂缓新训练做确认/合规。空槽后GPU强制全局400/micro100八路由两步完整状态，再原入口两步/val2000检查初始四指标与V250差<=1e-4，最后原父fresh18000步。输出/计划/锁/失败/缓存排他，不覆盖、不从探针续训、不因等待超时重启。

预留formal artifacts/pure_neural_v259/eight/global400_steps18000；原入口probe artifacts/resource_probe/v259/eight_global400；label v259_eight_rms_global400_18k。当前没有CPU/GPU结果、完整执行器或运行队列，实施完成后另记。

## 判定

完整18000步/73点、720万抽样、800UE全局损失、micro100/验证batch100、固定源文件/初始一致通过后，同split1176/offset2000/2000信道/noise22701/2/3完整1152位audit。相对V250三组总分均升且信道聚类bootstrap95%下界>0才登记新冻结确认；若主干变化另补最新冠军，不更换原对照。不把重复选型audit或有历史交叉的窗口称全项目盲测，祖先来源限制保留。最终确认、实际包大小/哈希/接口/回归/Git发布通过才晋级，不自动上传，不将本地69说成线上69。
