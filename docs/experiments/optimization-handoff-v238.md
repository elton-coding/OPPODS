# 持续优化交接：V234—V239

更新时间：2026-09-09 04:09（北京时间）。此页是恢复工作的索引，不替代实时进程检查。

## 当前结论

main与canonical提交包仍为V230E，本地固定audit均分68.360464，尚未达到69；唯一由用户确认的线上成绩仍为V223的67.01164987745。V233伙伴SNR完整复评68.334231、V235共享Encoder解冻68.311847，都没有超过E；相对同预算C的95%区间均跨0。完整证据与解释已提交ddf2b02，见各自实验报告。

当前开发分支codex/pure-neural-scale-invariant-loss-v239包含V237/V238/V239运行器和全部依赖。不要切换到会删除待启动实验源码的旧分支。V239使用独立训练入口，不改正在运行的基础训练器、V227/V230/V238设计。全仓92项测试通过、10项历史物理测试跳过，Ruff通过。

## 活跃训练与顺序队列

| 队列 | 当时进程/会话 | 已确认状态 | 自带后续动作 |
|---|---|---|---|
| V234反馈门控 | 已完成，旧会话89470 | audit68.314231；相对C+0.000332区间跨0，低于E | E额外配对已做；证据已提交f4a991a，不再重跑 |
| V236低SNR k4 | 已完成，旧会话55269 | audit67.735030；相对C-0.578869，P10下降，拒绝 | 证据已提交f4a991a，不再重跑 |
| V237两专家5e-5 | 已完成；旧会话50692已终止 | audit68.229287，比C-0.084612，拒绝 | 证据v237_two_lr5_audit_offset2000.json及3个NPZ，不重跑 |
| V237八专家5e-5 | 训练PID23852，父等待器36484；会话68524 | 5000/12000步，当前最佳仍起点68.023301 | 完整训练及audit，标签v237_eight_lr5；比两专家慢，不因此重启 |
| V238宽度1024 | 训练PID61756，父等待器60764；会话92441 | GPU探针通过，正式3000/12000步，最佳validation68.089083 | 从原V227完整训练后自动audit，标签v238_width |
| V239 RMS评分代理 | 等待器PID32588；会话91324 | CPU探针通过，等待V237-eight完整audit | 若其<69，正式12000步训练并audit，标签v239_rms_score |

PID/会话只供定位，恢复时必须核实实际命令行和存活状态，不可因编号相同就操作进程。最多两项GPU训练。队列已经存在，不再次启动相同命令。V237运行器有每组独立锁、三小时依赖等待超时、正式目录存在即拒绝续训，并保存输入哈希；失败需先诊断，不能直接删输出重跑覆盖证据。

V234额外E配对已完成：-0.046233，95%区间[-0.083723,-0.021029]，见benchmarks/v234_vs_v230e_paired.json。V236及V237自动audit同时比较C和E。不要在训练尚未完成时用正在更新的权重做最终复评，也不要复制中间权重冒充最终最佳。

V237输出分别为artifacts/pure_neural_v237/two/lr5与eight/lr5。2×2对照为C=V230C、A=V230E、B=V237-two、AB=V237-eight。已有compare_factorial_holdout.py可算交互，但其一个键名仍带旧实验hard_rank语义，不能在新报告中照搬误标；可添加清晰的新语义键并保留兼容后测试。

## 正式训练中的V238

research/pure_neural_v238/modelDesign.py已实现宽度512→1024，保持父函数的通道复制/正负扰动初始化，训练激活重算；174338872参数。真实V227父权重CPU合成输入已复核：6912个logit最大差6.3211e-5，硬判决差异0，证据v238_parent_parity_cpu.json。GPU batch100探针通过，峰值分配4.99GB、保留5.90GB；正式validation2000起点68.02328529357911，与C一致。尚未完成正式训练、最终复评或提交包。

scripts/run_pure_neural_width_v238.py已在V237-two复评后完成独立GPU探针并启动正式训练，当前会话92441。artifacts/pure_neural_v238/wide重新从原V227初始化，不使用探针更新权重；1e-5/12000步完成后自动v238_width三噪声audit。不得重复启动或拿资源探针充当效果；不混入高学习率。探针已留档benchmarks/v238_gpu_resource_probe.json。

## 已排队的V239

scripts/train_pure_neural_rms_v239.py只在本进程执行main时替换解析和损失，基础训练器文件未变。评分项使用可导逐UE RMS归一化（sqrt前clamp均方至1e-8，RMS下限1e-4），BCE保持原始logit；模型和其他配置完全等同C。报告loss_kind=rms_score，不误写soft_score。6项测试及真实父模型CPU两步训练通过，probe证据v239_cpu_training_probe.json不是效果指标。

会话91324的scripts/run_pure_neural_rms_v239.py正在等待v237_eight_lr5完整audit；若其仍<69，则从原V227初始化artifacts/pure_neural_v239/rms_score并正式12000步训练，之后自动同时对比C/E。若>=69则先冻结确认而暂缓新训练。等待不占GPU，仍最多两项GPU训练。正式目录若存在拒绝隐式续训；指纹包含基础训练器、独立入口、设计及三份父权重。不要重复启动或修改这些在训/待启动依赖。

## 后续检查与停止条件

本任务的30分钟自动续跑已创建并通过查看验证，id为oppods-69。它负责继续检查、复评、实验决策和实现，不只是重复日志。训练无变化时保持安静，实质结论或失败才通知。需本机开机、应用保持运行。不要创建重复自动任务。

当前audit固定split1176、test offset2000、noise22701/22702/22703，已经被查看用于选型。超过69附近/以上时先冻结候选，再核实未用于本轮选型的后续测试窗口及新噪声做确认，谨慎审计历史重新切分可能造成的通道重叠。只有经过固定audit、冻结确认、合规打包及测试的改进才能发布；本地和线上严格区分，不自动上传赛站。若本地固定audit和冻结确认均超过69，提供包与证据，暂停续跑等实际提交反馈，不宣称线上已达69。
