# V262：共享前层 × Encoder解冻的完整2×2消融

2026-09-10预登记，分支codex/pure-neural-shared-endtoend-v262，从e56c0eb创建。已有两项同预算、相同父初始化的正向选型信号，新增组合AB检验交互，而非直接叠加收益。主干仍V250，以下候选均未确认或证明线上超过69。

| 组 | 共享前8层 | Encoder训练 | 完整72k固定audit |
|---|---|---|---:|
| C：V250 | 否 | 否 | 68.60602864583332 |
| A：V257 | 是，同父共享 | 否 | 68.6505982349537 |
| B：V258 | 否 | 是，统一Adam1e-5 | 68.65581090856482 |
| AB：V262 | 是，同父共享 | 是，统一Adam1e-5 | 尚未训练 |

A-C +0.044570、B-C +0.049782均达到各自三噪声总分均升/配对区间下界>0的选型入口；B-A仅+0.005213且区间跨0。V257确认已独立冻结并等待；V258确认尚未登记。它们不是已证明可叠加的效果。AB相对A只增加Encoder训练，相对B只增加共享前层；相对C明确是双因素组合，不冒充单因素。

## 固定训练与证明

原V227独立初始化，父映射[0,0,1,1,1,1,1,1]，fresh Adam；不得从A/B/C或探针权重续训。设计直接使用冻结research/pure_neural_v257/modelDesign.py，Encoder+Tx+Rx全部训练，预计73547840可训练参数、532个去重参数张量。八个等宽5dB joint-min-SNR区间、512宽10块、全1152位/k8不变；低父2专家/高父6专家各共享输入及前8块，后2块/输出保持独立，不跨父共享。

与原72k队列一致：batch100、seed15240、全SNR均匀、RMS T0.5/公平0.3/带宽0.025/原始BCE0.05、恒定Adam1e-5、72000步、验证每1000步2000信道、patience72、FP32/cap0.4。不与余弦、低温度、大批次、Rx自身路由或硬排名组合。

先CPU原训练信道八路由证明：与A的冻结Encoder初始输出/损失/裁剪前TxRx梯度相同，解冻Encoder梯度非零且真实两步更新；与B的独立专家相比，初始输出/损失相同，Encoder梯度一致，共享参数梯度等于同父独立参数梯度之和、私有参数梯度一致。检查构建和显式噪声随机流、去重、共享别名及两步完整Adam状态，不能把运行证明叫分数。再空槽GPU100信道八路由两步状态/共享检查，原入口两步/val2000初始四指标与C差<=1e-4，最后原父fresh完整72k。

预留formal artifacts/pure_neural_v262/eight/shared_endtoend_steps72000，probe artifacts/resource_probe/v262/eight_shared_endtoend，label v262_eight_rms_shared_endtoend_72k。现有V259/V260训练、V261等待和V257确认不动；AB正式队列须等V261真实完整审计及V257冻结确认完成后再申请GPU，最多两项重任务。不得绕过V261的已知路径键故障，把其失败/两步探针误当完整预算。代码、证明和运行器完成后再登记冻结输入并排队，此刻都尚未实现或启动。

## 固定评测、交互及晋级

完整73点、准确训练组件/参数数/源SHA/初始指标通过后，固定split1176、offset2000、2000信道、noise22701/2/3、全1152位，对C/A/B逐信道配对。报告AB-C、AB-A、AB-B，以及(AB-A)-(B-C)交互；四组同一信道索引跨噪声共享聚类bootstrap2000次，不用不配对的均分直接算不确定性，不校正反复选型的限制仍披露。

只有AB对C/A/B三者分别三组总分均升，且各自配对95%区间下界>0，才进入另行预登记确认；交互值正负只解释是否叠加，不单独代替候选性能门禁。届时冠军有变化另补比较。即使平均超过69，也须冻结确认和实际ZIP/四成员SHA/CRC/接口/回归/Git发布检查通过，不能将本地评分称线上成绩。历史信道交叉及祖先训练来源缺口继续披露。无自动晋级或赛站上传。
