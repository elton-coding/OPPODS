# V260：只改变接收端的SNR专家路由

## 2026-09-10 10:44：真实CPU证明、执行器与完整回归通过，尚未排队

设计/协议先在00169b4提交并推送，随后CPU会话38877正常exit0，耗时21.829766099806875秒。证据benchmarks/v260_cpu_receiver_routing_probe.json：8个原训练信道覆盖Tx八路由各1，Rx路由计数[2,2,1,1,1,1,1,7]；单信道输出差0、batch8最大logit差4.76837158203125e-6、硬判决差0、loss差0，构造/显式噪声RNG相同。Tx全部梯度最大差1.6298145055770874e-9，Rx按同父组求和梯度最大差2.3283064365386963e-9。Rx独立专家的梯度分配本来就改变，包括对后续全局梯度裁剪的影响，不能称整个优化轨迹相同。

两步真实Adam均1296完整有限状态，189551584可训练参数；Encoder冻结且权重逐位不变。CPU完整state_dict内存序列化761895939字节后恢复，所有参数和全链路输出相同；这是运行证明，不是合规ZIP大小，也没有保存/复用探针权重。38项专项/Ruff通过；完整CPU回归会话34768正常exit0，307通过10跳过、39.50秒。真实CPU记录已再次通过生产require_runtime及12项来源SHA门禁；现有V259的79项冻结输入和V258的9项完成前驱再次SHA通过，未干扰正在训练的模型。

独立执行器scripts/run_pure_neural_receiver_v260.py已实现：等待V258完整72k审计并冻结四文件/报告/审计/三缓存，仅作为资源依赖；按预登记先全状态GPU检查、原入口两步/val2000起点检查，再原父fresh72k和固定审计。额外要求实际输出modelDesign.py与计划中V260源码哈希一致，防止只有参数报告一致却运行了其他结构。未满足前驱/源码/预算/路线/梯度/初始化等检查不放行，部分结果保留且不隐式重启。当前还没有实际创建排队计划或GPU成绩，先提交本轮实现及证据。

设计SHA478e5cd715941d2f7558e2038c678564fd552cdb89b886acccb0c295a77b5a1e；CPU/GPU证明脚本SHA85d16edcbf595b996b35c25dde9d611f3c87c47bcd5ccd8888f2fbdff8ccca03；执行器SHAf60d7f8b115c5a3fa3695ff675b75fadc0bcaa56618ec6a9688a5b7456687883；CPU证据SHA90bafe05a68e47d2e65e3a2e29097766ddc67713fc42a5cb47021574a4ceaad6。正式目录artifacts/pure_neural_v260/eight/receiver_own_steps72000、原入口探针目录artifacts/resource_probe/v260/eight_receiver_own、标签v260_eight_rms_receiver_own_72k。以下“没有证明/没有执行器”是初始登记状态。

2026-09-10预登记。分支codex/pure-neural-receiver-own-snr-v260从4151b4a创建。主干仍V250本地68.60602864583332、线上未知；V256温度减半完整负结果已否决。现有V257/V258继续训练，V259继续等待。本实验尚无训练、成绩或CPU证明；先登记并推送假设和设计，再运行独立CPU证明。不修改已冻结生产文件。

## 唯一因素

当前八个Tx专家按pair-min-SNR的5dB区间工作，Rx用控制位直接选择同编号专家。V260保留Tx/Encoder及控制位逐字节逻辑，只把八个Rx专家改为“保持原V227父组，再按自身SNR分工”。两组定义由原父模型边界-10dB决定，不从新候选分数搜索：

| Tx控制编号/原父组 | 自身SNR | Rx编号 |
|---|---|---|
| 0或1（pair-min<-10） | [-20,-10) | 0 |
| 0或1（pair-min<-10） | [-10,20] | 1 |
| 2至7（pair-min>=-10） | [-10,-5)、[-5,0)、[0,5)、[5,10)、[10,15)、[15,20] | 2至7 |

发送方与接收方的父映射都仍为[0,0,1,1,1,1,1,1]，所以初始每次选择的Rx父权重与V250对应父组相同，无需改训练入口或初始化映射。八个Rx仍独立，数量/宽512/深10/190354976总参数/189551584可训练参数均不变；不引入控制嵌入、物理模块、共享前层、Encoder解冻或新温度。

对独立均匀[-20,20]的两UE SNR，按UE计数的Rx路由概率为[1/4,3/16,3/32,3/32,3/32,3/32,3/32,3/32]；原pair-min路由为[15,13,11,9,7,5,3,1]/64。这里是给定分布下的解析概率，不是用测试集拟合采样比例。训练仍原分布，不重采样。该单因素同时改变Rx条件信息的分区方式、分区分辨率和各专家曝光量，不能分别作因果归因。

假设：Rx按自身噪声条件分工、减少极稀疏专家可能改善当前八专家长训练。风险：同一Rx开始接收多个Tx专家生成的信号，而ReceiverCore仍不额外读取Tx控制嵌入；这种混合可能更难学习。此外低父组的弱UE从两个5dB min区间合为一个自身10dB区间，可能损害尾部表现。必须完整训练比较，不能由初始等价或更均衡概率推断提升。

## 固定训练及运行证明

仍原V227 fresh初始化，不从V250或探针续训；原RMS T0.5/公平0.3/带宽0.025/BCE0.05、Adam1e-5、batch100、72000步、验证2000每1000步、patience72、seed15240、完整1152位/k8、Encoder冻结、FP32/cap0.4不变。唯一命令变化model-design和output-dir；来源、控制和实际结构单独哈希绑定。

CPU使用split1176训练前8个真实信道、交替弱UE身份和八个Tx中点、伙伴20dB，覆盖全部八个Tx/Rx路由。单信道全链路初始输出需逐位一致；batch8允许不同分组GEMM数值差<=2e-4、loss差<=1e-6、探针硬判决无变化、构造及噪声RNG相同。Tx所有梯度保持atol1e-5/rtol1e-4；Rx因重新分组，各独立专家梯度不应逐一相同，只要求按同父组求和的全部梯度在同容差内一致。两步真实Adam须1296参数张量/状态完整有限，Encoder保持冻结不变，序列化恢复输出/参数一致。密集SNR边界、5bit控制解码和实际Receiver输出路由另外测试。

后续执行器预定等待V258完整72k审计释放槽，前驱仅资源依赖。若前驱或对照已>=69，暂缓新训练先确认。先GPU强制100信道全八路由两步完整状态/cap0.4，再原入口两步/val2000初始四指标与V250差<=1e-4，最后从原父重新训练72000步。输出/锁/证据排他，超时不隐式恢复/覆盖。当前尚未编写或启动执行器，不新增第三项GPU工作。

## 评分与采纳

完整73点/预算/全1152位/实际四文件哈希及初始一致通过后，固定split1176 offset2000的2000信道、噪声22701/22702/22703，对注册V250作信道聚类配对bootstrap。三组总分均升且95%区间下界>0才进入另行预登记的新确认；若主干已改变另补对新主干比较。预登记确认、包大小/四文件接口/CRC/完整回归和Git发布验证通过才可晋级，不自动上传，不把本地69称线上69。

当前选择集、已用offset4000/6000以及历史通道重叠/祖先来源缺口继续披露，不声称全项目盲测。V250 P10诊断只是重复选择集描述，不能算新确认；本路由边界取原父组和5dB网格，没有用新候选测试分数搜索网格。
