# V128 Walsh 尾部双区间独立门限（中性）

日期：2026-08-24  
分支：`feature/walsh-tail-two-bin-v128`  
对照：V127

将 `[-7.5,-5)` 与 `[-5,-2.5)` 的截断均值门限独立搜索，并要求六个种子相对 V127 均不退步。

结果显示：前一区间门限在 `0.245–0.300` 内没有改变六种子的任何输出；后一区间唯一非劣圆整点仍为 `0.260`。因此 V127 已处于稳定平台，没有可登记的分数增益。

候选中性，不修改模型、不创建新版本。
