# V248：评分代理温度0.5→1.0单因素消融

## 完成结论（2026-09-09 13:30）

完整12000步，最佳step11000、validation68.15459938049315；耗时1952.987519秒，峰值分配7318530048字节。固定audit精确均分 **68.26968171296296**，三噪声final分别68.07008463541666、68.31398003472222、68.42498046875；效率73.61946614583334、73.93068576388889、74.0892578125，P10分别55.12152777777778、55.208333333333336、55.208333333333336。

对同预算C，差-0.044217521，95%区间[-0.058261016,-0.007144274]为负。三组效率虽上升，P10下降使总分均下降，不采纳T1。对新冠军V242E的原offset2000配对差-0.261944123，区间[-0.290329169,-0.223604235]，不晋级、不打包。这只检验原soft_score上的温度变化，不能外推RMS归一化等未匹配目标下的所有温度选择。

4文件哈希已与最终audit逐一核对一致，证据benchmarks/v248_temperature1_audit_offset2000.json、3NPZ、v248_vs_v242e_paired.json。会话68158正常结束，训练PID59348/父63120已退出，不重复运行；未使用V242E确认窗口拟合、选择或比较本候选。以下保留预登记和历史执行状态。

12:58更新：V242E完整audit68.531625，前驱两项预留比较已自动完成。GPU batch100两步探针通过（峰值分配7188857344、保留9919528960字节），证据benchmarks/v248_gpu_training_probe.json；正式12k从原V227开始，训练PID59348、父63120、会话68158，step0四项validation与C一致，final68.02328529357911。当前不是等待器；不重复启动、不使用V242E/probe权重续训。当前主干仍V240C，V242E待另一窗口冻结确认；本实验结束需检查届时冠军再比较。

预登记：2026-09-09 12:17；尚无正式效果分数。当前研发分支codex/pure-neural-temperature-v248继承8f68712，保留在训V242/V247依赖，不修改它们的源码。

12:18执行：全套测试通过、10项跳过，新增代码Ruff通过，预登记与代码47b8f57已推送。等待器PID63120、会话68158已启动，当前尚未运行GPU探针或正式训练。它会生成下列两个前驱比较报告，不再手工重复写入；当前显卡仍只有V242和V247两项训练。

## 假设与边界

V231曾把温度0.5降至0.1，在相同12k对照下净-0.085636且配对区间全负。低温近似硬判决更紧，并不意味着更易优化。V248只检验反方向T=1.0：更平缓的sigmoid是否改变当前logit尺度下的梯度分布、改善完整评分。不声称一定增加所有错误bit的梯度，也不保证分数随步数单调。

温度同时影响每个UE的软分数及其软P10排序，不能只归因于判决边界的梯度宽度。提高T会降低边界附近sigmoid导数，且与固定BCE权重有相对尺度效应。这是一个温度超参数消融，不是纯粹控制梯度范数的实验。当前梯度机制均可由已有损失定义直接验证，不引入物理接收机、解析预编码或推理后处理。

## 控制变量

相对V230C，唯一超参数改动score_temperature=0.5→1.0。均从原V227两专家完整初始化，Encoder冻结、Tx/Rx全部训练，mapping[0,1]、k8/B1152、seed15240、batch100、Adam1e-5、12000步/120万训练信道抽样、完整SNR分布、soft_score/P10权重0.3、带宽0.025、BCE0.05不变；validation2000每1000步，patience12。不是从V242、V247或资源探针继续训练。无需修改模型设计或基础损失代码。

对照C固定audit68.313899，发布冠军V240C为68.439628。前者用于同预算方法效应，后者用于发布决策，不把36k训练和12k直接当成等预算方法对照。V231 T0.1结果作为历史负方向记录，本次只预登记一个新温度，不先看评测再搜索网格。

## 队列和证据

新运行器scripts/run_pure_neural_temperature_v248.py等待V242-eight完整36k及三噪声audit；等待不占GPU。不使用旧12000步依赖门禁。先在CPU完成前驱的同36k RMS对soft_score配对与完整八专家loss×budget 2×2：C=V230E、A=V241 RMS12k、B=V240-eight soft36k、AB=V242-eight RMS36k。两个输出预留为benchmarks/v242_eight_rms_vs_soft_36k_paired.json、v242_eight_loss_budget_factorial.json，等待器负责生成，不另开重复写入。若前驱本地达到69则暂缓T1训练，先冻结确认。

释放槽后，完整GPU batch100两步资源探针输出artifacts/resource_probe/v248/temperature1；通过后才从原V227正式12k，输出artifacts/pure_neural_v248/temperature1。探针记录只证明运行与资源可行，不用其4通道分数判断效果。独占锁、已存在目录拒绝续训、启动前后源文件与父权重哈希防止覆盖和实验漂移；首次验证四项指标须匹配C。

完成后自动固定split1176/offset2000/每噪声2000通道、noise22701/22702/22703官方兼容batch1 audit，标签v248_temperature1，对C和V240C做通道聚类配对区间。仅有单次训练验证峰值不够；若届时冠军改变，须补最新冠军配对。任何可靠组合需另做消融，不能把温度、专家数、RMS和预算增益直接相加。

若达到69附近，先冻结候选再确认，遵守V243历史通道重叠与祖先来源记录缺口限制：当前audit经过反复选型，并非全项目未见盲测；本地69也不等于线上69。保留主干V240C直至合规和完整复核证实更优。
