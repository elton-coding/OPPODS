# V257：八专家共享前八层，保留独立后两层

2026-09-10 05:06 实施覆盖：预登记 13a1f9e 已先推送。真实父权重 CPU 证明完成（原会话96889正常exit0）：16 UE/八路由逐样本和批量 logits 差0、硬判决差0、RMS loss差0、构建及噪声随机流相同；全部共享梯度与原独立专家同组梯度和的最大差9.313225746154785e-10。有效总参数73547840、可训练72744448、唯一可训练参数张量528；两次真实 CPU Adam 均528完整状态和有限梯度，保存重载后参数/输出/共享别名相同。序列化整个链路294515031字节仅为CPU证明，不是正式四文件ZIP，不能替代发布验包。证据 benchmarks/v257_cpu_shared_prefix_probe.json；设计SHA aee1776eb1e8bc267a41a2c0532fd7a20673a0a906f88e0a4476f760029a3f46，CPU脚本SHA 9657b3a70bfc97597f7164511e43dd3901ec5494ac9a70e3943c35e42504b591。

运行器 scripts/run_pure_neural_shared_v257.py 和完整八路由GPU容量探针 scripts/probe_pure_neural_shared_memory_v257.py 已完成代码。13项专项及Ruff通过；完整CPU回归会话89473正常exit0，237通过10跳过（较早16218的230通过10跳过也已结束）。前序63项输入SHA复核未变。此刻尚未启动V257等待器/任何GPU动作；先随本记录提交推送，再启动排队。以下初始“尚无排队器”已由本段覆盖为实现但未启动。

队列预先冻结现有V256计划及其依赖、原V227/V250和V257代码/CPU证明；等待 V254 storage/decision.json 完整生成，不以A训练结束或单个audit代替四组完成。之后复核C/A完整72k、B/AB转换来源/全1152缓存、四组均分、实际A/B/AB ZIP CRC及哈希，冻结完成依赖。任一组本地>=69则先暂缓候选，由root处理确认/合规，不自动认定超限A可提交。否则空槽后强制batch100八路由[13,13,13,13,12,12,12,12]、两次Adam528状态、GPU迁移后1024项共享别名检查；再原入口batch100/val2000两步探针与V250初始四指标<=1e-4，最后原V227 fresh完整72k。输出排他，失败保留现场、不隐式恢复、不抢占存储审计槽。当前没有V257分数。

2026-09-10 预登记；特性分支 codex/pure-neural-shared-prefix-v257，从 d50180e 创建。当前仅准备代码和 CPU 证明，尚无 GPU 探针、正式训练、排队器或新成绩。现有 V254/V256 两项 GPU 训练保持原样，主干仍 V250 本地 68.60602864583332，线上未知。

## 单因素和假设

对照为 V250 的八个 5dB 联合 min-SNR 专家。只改变同一 V227 父模型内的参数共享方式：两个低区间共享一套输入嵌入及前八个残差块，另六个区间共享另一套；每个区间的最后两个残差块和输出头仍各自独立。Rx 的最终 LayerNorm 也独立。Encoder 沿用原文件并冻结，不跨 -10dB 父边界共享。

这是“前缀共享深度 0→8”这一结构因素，不是增加物理模块、低秩适配器、集成预测或压缩后冒充重新训练。输入宽度、层数、每条路径的算子、路由边界、控制位和 B1152/k8 全载荷不变。假设共享训练经验有助于降低稀疏区间的数据需求；风险是不同区间的梯度互相干扰、共享容量不足，不能预先认定更小就是更好。有效参数量和 Adam 状态量的变化是该因素的直接后果。

全部八个原始核心先正常构建，再绑定共享模块，避免额外改变初始化随机流。原 V227 父映射严格为 [0,0,1,1,1,1,1,1]。初始权重数值及八条计算路径与 V250 一致；训练中共享参数接收同组梯度之和，只进入 Adam 一次。源 V230/V250 及在运行的 63 项冻结依赖均不得修改。

## 预定训练协议

候选从原 V227 独立初始化、fresh Adam，而非当前冠军或 CPU/GPU 探针权重续训。与 V250 同为 RMS 评分损失 T0.5、公平权重0.3、带宽0.025、原始 BCE0.05、恒定 Adam1e-5、batch100、seed15240、全 SNR 均匀、Tx/Rx 训练、Encoder 冻结、完整72000步、patience72、每1000步2000验证信道、FP32/cap0.4。不与 V255 余弦或 V256 温度组合。

正式目录预留 artifacts/pure_neural_v257/eight/shared8_steps72000，GPU 探针 artifacts/resource_probe/v257/eight_shared8，正式标签 v257_eight_shared_prefix8_rms_72k。只有 CPU 证明、专项和回归通过、预登记推送以及空闲 GPU 槽门控后才能启动。当前不启动，不抢占 V254 存储四组审计的后续槽位。训练运行器与冻结排队依赖另行完成并记录；没有实现的等待器不得称已排队。

## 验证和采纳门禁

CPU 使用同一组训练信道、随机 bits/SNR/噪声，覆盖全部八路由，检查真实父权重下逐样本及批量初始 logits、硬判决、RMS loss、噪声随机流一致；逐张量核对共享参数梯度为原八专家同组之和、私有参数梯度不变；检查参数去重、Adam 状态和保存重载共享关系。初始数值证明不是效果分数，不能以参数减少宣称提分。

GPU 空槽后原入口 batch100/val2000 两步探针，初始四指标与 V250 差 <=1e-4；还需证实搬到 GPU 后绑定关系、全部八条路径完整 Adam 状态和有限梯度，不用“某随机批次没覆盖稀疏路由”代替资源验证。正式需完整73点/冻结输入/参数数/初始检查，再固定 split1176、offset2000、2000信道、noise22701/2/3 的全1152位审计。

与 V250 三组总分均上升且按信道聚类配对 bootstrap 95%区间下界>0，才进入新的预登记冻结确认；当时冠军若变化另补对新冠军，不更换原对照。完整确认、实际 ZIP<=1e9/四成员哈希/接口/回归/Git 发布门禁后才可晋级。offset4000/6000已用于确认，历史信道交叉及祖先来源缺口仍披露，不声称全项目盲测或把本地69当线上69。不自动上传赛站。
