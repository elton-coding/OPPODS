# V261：当前八专家RMS条件下的硬排名替代梯度

2026-09-10预登记；分支codex/pure-neural-rms-hard-rank-v261从4eb01a7创建。V257/V258继续训练，V259/V260继续等待。本实验先实现与CPU证明，不占第三项GPU；当前没有CPU证据、执行器、等待队列或分数。主干V250本地68.60602864583332、线上未知，69未达。

## 旧证据与新假设

V228不是当前RMS八专家长预算：其真实记录为原单模型、全部模块训练、V224父权重、未归一化soft/hard_rank、Adam5e-6、6000步、seed15234。硬排名均分68.127341相对同预算soft68.119797仅+0.007544，区间[-0.016267,+0.028037]跨零。V229的联合低模式方法×硬排名也是6000步，AB-A=-0.010859、区间跨零，没有证实正交互。这些结果保留，不能当成当前条件的提升证据，也不能误记为八专家RMS72k已经试过。

刚完成的冻结V250训练划分诊断发现：整体排序相关很高，局部硬q05–q15仍有错排；该局部检查是事后描述，不是泛化证明。温度0.25改善诊断数值但V256完整重训失败，因此V261不再改变温度或带宽，只检验硬排名在当前RMS条件是否影响有效训练。不是直接采纳旧V228或把其+0.007544加到主干。

## 唯一因素与明确限制

原RMS归一化及原始BCE不变，替换soft_score分支为既有hard_rank_score分支：每位前向按(logit>=0)==bit硬判决，反向借用相同T0.5的sigmoid梯度；逐UE前向正确率使用真实硬正确率，排序也按硬正确率。P10前向用精确线性插值分位数，反向仍使用tail0.1、bandwidth0.025的Gaussian秩权重，避免仅两条UE接收分位梯度。BCE保持未归一化原始logits、权重0.05。

去掉BCE项后，前向目标在浮点容差内是该批次真实负评分；加BCE后当然不是纯负评分。反向是有偏替代梯度，不是硬评分的数学导数，不能通过通常的有限差分gradcheck来证明，也不保证任何训练/验证/线上分数单调升高。前向数值更真实本身不是收益。这里同时改变排序和前向计分定义，是一个已明确的损失定义因素；不将两者拆成独立收益。

其余完全固定V250：原V227 fresh Adam初始化、八个5dB pair-min Tx/Rx专家、父映射[0,0,1,1,1,1,1,1]、Encoder冻结、512×10、189551584可训练参数、全1152位/k8、batch100、Adam1e-5、72000步、seed15240、全SNR独立均匀、验证2000每1000步、patience72、FP32/cap0.4。T0.5/公平0.3/BCE0.05/带宽0.025均不变，不叠加共享前层/解冻/大批次/新Rx路由/余弦。

## 检查与执行协议

隔离入口train_pure_neural_rms_hard_rank_v261.py仅在自身进程替换函数并在成功/异常时恢复，报告必须标记rms_hard_rank_score，不伪称rms_score或普通hard_rank_score；现有共享源码不改。CPU单元验证完整100×2×1152、零/近零/通常/大幅logits下有限前向/梯度；前向与硬mean/P10+BCE对齐；逐logits梯度与独立写出的固定硬排名Gaussian权重软RMS参考一致；构造软硬排名相反的例子，确认公平项梯度确实改变，而公平权重0时两种损失的梯度保持一致。

真实CPU使用split1176训练前8信道、交替弱UE与伙伴20dB、覆盖八路由，两步Adam须1296完整有限状态、Encoder冻结不变；每步前向硬评分契约与logits级手工替代梯度匹配、loss不消耗RNG，不保存权重。GPU空槽后强制100信道八路由两步同类全状态证明，再原入口两步/val2000初始四指标与V250差<=1e-4，最后原父重新初始化正式72k，不使用探针权重。

预定资源依赖为V259完整18000步/720万抽样固定审计，仅释放槽而非训练对照；必须验证其全预算报告及四文件/审计/三缓存后冻结来源。若注册对照或前驱已本地>=69，暂缓新训练先确认。输出/计划/锁/失败证据排他，不覆盖或因等待超时隐式重启。当前执行器尚未编写/启动，旧队列不受影响。

## 评分与采纳

完整73点、原loss_kind/预算/架构/初始一致/全载荷/冻结源码通过后，固定split1176 offset2000、2000信道、噪声22701/22702/22703，对注册V250做按信道聚类配对bootstrap；三组总分均升且95%区间下界>0才进入另行预登记的新确认。届时主干若改变另补比较，不替换注册对照。确认、四文件接口、实际ZIP<=1e9/CRC、完整回归及Git发布门禁通过才晋级，不自动上传。

原选择窗口和offset4000/6000不能称新鲜盲测，历史信道交叉/祖先来源缺口保留；400训练信道诊断也不能变成确认集。所有本地结果与平台成绩严格区分，不以单次验证峰值或“损失前向恰为硬分数”宣布达到69。
