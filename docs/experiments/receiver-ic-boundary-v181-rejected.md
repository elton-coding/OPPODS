# V181 Receiver 干扰抵消最低启用边界（拒绝）

日期：2026-08-25  
分支：`codex/receiver-ic-boundary-v181`  
对照：V156，`INTERFERENCE_CANCELLATION_MIN_SNR_DB=-10.0`

seed1176、500 样本官方兼容 exact 评测；V156 final 为 `62.844883`。

| 候选边界 | 改变区间 | final | 相对 V156 | P10 变化 |
|---:|---|---:|---:|---:|
| `-10.50` | 在 `[-10.5,-10)` 新增抵消 | 62.843303 | -0.001580 | 0 |
| `-10.25` | 在 `[-10.25,-10)` 新增抵消 | 62.842391 | -0.002491 | 0 |
| `-9.75` | 在 `[-10,-9.75)` 关闭抵消 | 62.842878 | -0.002005 | 0 |
| `-9.50` | 在 `[-10,-9.5)` 关闭抵消 | 62.843546 | -0.001337 | 0 |

四个预定义窄区间在首个发现 seed 全部回退，不扩样、不进入盲测、不修改
模型；保留 V156 的 `-10.0 dB`。已有 `[-8.5,-8.0)` 禁用窗口不受影响。
