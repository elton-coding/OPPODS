# V274：当前主干评分代理诊断，非训练

对已冻结V269和V273做固定split1176 train/validation各前2000信道，三噪声27421/27422/27423；batch100，无参数更新、无优化器、无检查点输出。两划分ID互斥，两模型和划分使用位置匹配的比特/SNR/噪声随机流。预先登记后运行，不按评分挑样本。

记录真实硬判决综合分/P10、BCE、逐UE RMS代理正确率的分布及其与硬评分的相关、硬尾部的代理尾部召回/精度、P10附近误差；逐batch记录实际原训练目标及硬综合分。代理指标按全2000信道汇总只作诊断，不能冒充训练batch级秩目标；实际训练目标另按100信道原函数计算。

目的：检验预算增长的收益是否伴随代理与硬P10的一致改善，定位可能失配，不因代理绝对值低于硬分就认定失效。两固定检查点比较不是训练全轨迹，不证明因果；验证集曾选checkpoint、batch100不同于正式batch1，结果不能晋级或作为线上分数。不得据此调测试格子拼接。若无明确失配，不直接开新损失扫描。
