# V242E冻结确认预登记

发布完成更新（2026-09-09 13:33）：全套测试在修正冠军元数据一处哈希抄写错误后重新通过，实际模型字节始终与冻结源一致。V242E发布提交bb137e4、元数据145e1b6已连同两份新LFS权重上传，main与origin/main已同步，标签solution-v242e-score-68.531625已推送。模型、确认报告和历史限制不变；本地冠军发布不代表线上69达成。

## 完成结果（2026-09-09 13:19，发布校验随后进行）

全部六组顺序评测完成，计划绑定的源权重、源audit和评测脚本哈希在前后均相同；run_frozen_confirmation_v242e.py的最终判定supports_current_cohort_promotion=true。会话94884正常结束，不重新运行此确认。结果不是选择最高窗口得到：只有预登记offset4000和三固定噪声，没有换窗口或追加第三候选。

| 噪声 | V240C总分 | V242E总分 | V242E效率 | V242E P10 |
|---|---:|---:|---:|---:|
| 32701 | 68.21632595486112 | 68.25577256944445 | 73.81032986111111 | 55.295138888888886 |
| 32702 | 68.56978298611111 | 68.65700303819443 | 74.19750434027777 | 55.729166666666664 |
| 32703 | 68.50723524305556 | 68.62557725694444 | 74.11540798611111 | 55.81597222222222 |
| 均值 | 68.43111472800926 | 68.51278428819444 | — | — |

精确audit均值如上；NPZ配对均分差+0.081669430，95%通道聚类区间[0.063592213,0.127998089]，三噪声总分、效率和P10都提高。与原选型audit+0.091998的方向一致。通过的是本轮条件性确认，历史交叉1779/2000和祖先来源缺口没有因此消失，whole_project_blindness_certified、ancestor_training_provenance_certified、online_confirmation仍全部false；尚未达到69。

证据：benchmarks/v240c_confirm_v242e_audit_offset4000.json、v242e_confirm_audit_offset4000.json、各3NPZ及v242e_confirmation_decision.json。候选原始包和接口检查证据一并保留，后续本地冠军升级见V242E提交说明。下面保留原始登记和执行时间线。

登记时间：2026-09-09 12:58。此时尚未读取本次确认分数。脚本run_frozen_confirmation_v242e.py --register-only已生成benchmarks/v242e_confirmation_plan.json；当前main仍V240C，不提前晋级。

13:01执行：计划和全部选型证据8f1c564已先行推送，之后才启动确认。主运行器PID4836、会话94884；当前先运行对照audit（父41436，首噪声评测3156）。全套测试通过、10跳过，Ruff通过；确认开始后显存约26225/32607MiB。已有两项GPU训练V248/V249保持，确认只顺序推理，不新增训练。尚未得到完整确认分数，不重复启动或手工写入同标签输出。

13:05候选兼容性准备：在CPU严格加载全部190354976参数，检查25组SNR输入（8区间中心各两种强弱用户排列、9个相同SNR边界），50个UE输出均为1152个有限float32值，反馈96复符号、发射[batch,16,144]复数、5个二元控制位及全部8个专家路由均符合约定。证据benchmarks/v242e_candidate_contract.json含模型哈希。这是接口/路由检查，不是新的性能分数或远端运行时间认证。

候选包artifacts/FATE_MIMO_submission_pure_neural_v242_eight_rms_36k_official.zip已生成，707021014字节，小于1GB，SHA256 `c39e000ca8ef905563fbb95aaa7e7220b69cc94d5742fbba6541f3616866dd7c`。ZIP四个成员、CRC、解压后各文件大小和SHA256均与冻结audit一致；证据benchmarks/v242e_candidate_package.json。打包仅读取冻结源目录，没有改变确认输入、modelSubmit、canonical包或main。候选包仍待完整确认，未上传比赛网站；官方1000秒远端限制不能用本机并发评测耗时直接认证。

## 冻结候选与对照

只比较V242-eight RMS36k（候选简称V242E）与当前主干V240C。两者均已完整36000步，分别由其原validation选择best36000。使用原始不可变artifacts目录，不从正在训练的V248/V249取权重，不运行第三个候选。两个目录的四个模型文件及来源audit、评测脚本均在计划中绑定哈希，确认前后重新核对。任何哈希变化使本次确认失效，不换标签自动重跑。

候选原选型audit68.53162543402779，相对主干+0.091997522，配对95%区间[0.068086366,0.128838012]；同36k八专家soft_score对照增益+0.089305579，区间[0.055317469,0.127839778]。这些结果用于选定候选，不称确认结果。

## 一次性窗口与判定

- 固定split1176，test offset4000，2000通道，噪声32701/32702/32703，官方兼容batch1；不是换split_seed制造新留出集。
- 先对照三噪声，后候选三噪声，全部保存NPZ和完整精确指标；只有一个评测子进程顺序运行，保留两项现有GPU训练。
- 对照标签v240c_confirm_v242e，候选标签v242e_confirm。窗口、seed、候选与判定均在运行前冻结，已有输出即拒绝覆盖或自动重试。
- 当前队列支持晋级的预登记条件：三组配对总分差均>0，通道聚类bootstrap 95%区间下界>0。报告同时列效率和P10，不把单次P10跳档当作总分鲁棒性。
- 若未通过，保持V240C，不另挑4000/6000/8000中的最高窗口补救。同窗口后续若用于改模型，即为开发证据，不再称独立确认。

## 必须保留的限制

V243已发现offset4000与历史有ID评测记录交叉1779/2000。本窗口与当前split1176梯度训练、validation及本轮offset2000选型不交叉，但祖先训练执行谱系尚未完全获证；不能把当前分割无交集扩写为所有祖先无泄漏。它是“未参与本轮候选选型、但与更早历史评测交叉”的条件性确认，**不是全项目从未见过的盲测**。计划与最终判定无条件保留whole_project_blindness_certified=false、ancestor_training_provenance_certified=false、online_confirmation=false。

只有复核后才考虑作为本地冠军更新依据，不能据此宣告整个69分目标已完成。线上成绩仍只有用户确认的V223 67.01164987745；本地任何窗口达到69均不能直接代替正式排行榜。
