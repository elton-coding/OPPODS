# V242E冻结确认预登记

登记时间：2026-09-09 12:58。此时尚未读取本次确认分数。脚本run_frozen_confirmation_v242e.py --register-only已生成benchmarks/v242e_confirmation_plan.json；当前main仍V240C，不提前晋级。

## 冻结候选与对照

只比较V242-eight RMS36k（候选简称V242E）与当前主干V240C。两者均已完整36000步，分别由其原validation选择best36000。使用原始不可变artifacts目录，不从正在训练的V248/V249取权重，不运行第三个候选。两个目录的四个模型文件及来源audit、评测脚本均在计划中绑定哈希，确认前后重新核对。任何哈希变化使本次确认失效，不换标签自动重跑。

候选原选型audit68.53162543402779，相对主干+0.091997522，配对95%区间[0.068086366,0.128838012]；同36k八专家soft_score对照增益+0.089305579，区间[0.055317469,0.127839778]。这些结果用于选定候选，不称确认结果。

## 一次性窗口与判定

- 固定split1176，test offset4000，2000通道，噪声32701/32702/32703，官方兼容batch1；不是换split_seed制造新留出集。
- 先对照三噪声，后候选三噪声，全部保存NPZ和完整精确指标；只有一个评测子进程顺序运行，保留两项现有GPU训练。
- 对照标签v240c_confirm_v242e，候选标签v242e_confirm。窗口、seed、候选与判定均在运行前冻结，已有输出即拒绝覆盖或自动重试。
- 当前队列支持晋级的预登记条件：三组配对总分差均>0，通道聚类bootstrap 95%区间下界>0。报告同时列效率和P10，不把单次P10跳档当作总分鲁棒性。
- 若未通过，保持V240C，不另挑4000/6000/8000中的最高窗口补救。同窗口后续若用于改模型，即为开发证据，不再称独立确认。

## 必须保留的限制

V243已发现offset4000与历史有ID评测记录交叉1779/2000。本窗口与当前split1176梯度训练、validation及本轮offset2000选型不交叉，但祖先训练执行谱系尚未完全获证；不能把当前分割无交集扩写为所有祖先无泄漏。它是“未参与本轮候选选型、但与更早历史评测交叉”的条件性确认，**不是全项目从未见过的盲测**。计划与最终判定无条件保留whole_project_blindness_certified=false、ancestor_training_provenance_certified=false、online_confirmation=false。

只有复核后才考虑作为本地冠军更新依据，不能据此宣告整个69分目标已完成。线上成绩仍只有用户确认的V223 67.01164987745；本地任何窗口达到69均不能直接代替正式排行榜。
