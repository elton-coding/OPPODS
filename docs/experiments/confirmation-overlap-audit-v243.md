# V243：确认窗口历史交叉审计（评测工具，无新模型）

## 目的与方法

V240/V242长训练仍在向69推进。为避免将后续test窗口错误称为全项目独立盲测，先在不运行模型、不读取分数值的情况下扫描现存benchmarks与artifacts内所有NPZ的data_index，以及split_seed/noise_seed/test_offset元数据。只按通道ID计算交集，双UE及重复噪声去重。脚本scripts/audit_confirmation_overlap.py；快照benchmarks/confirmation_channel_inventory_20260909_0925.json。V243是评测审计版本，不是得分方案。

声明数据规模100000，与现有H_train和训练协议一致；固定split1176为80000训练、10000validation、10000test。工具只接收此总量，不读取信道数组，也没有改变任何在训依赖。若用于其他数据版本，必须先核实总量和样本顺序，不能照搬此结论。

## 实测交叉

扫描1590个NPZ，1566个具有通道ID，24个缺少ID，无无法读取文件。具有ID记录的通道并集89841；这是历次评测记录并集，不是当前训练集大小。

| 固定test窗口 | 通道数 | train1176交集 | validation1176交集 | 当前offset2000选型窗口交集 | 所有有ID旧评测交集 | 显式split1176旧记录交集 |
|---|---:|---:|---:|---:|---:|---:|
| offset4000 | 2000 | 0 | 0 | 0 | 1779 | 0 |
| offset6000 | 2000 | 0 | 0 | 0 | 1792 | 0 |
| offset8000 | 2000 | 0 | 0 | 0 | 1812 | 0 |

显式split1176仅指NPZ元数据筛选，不等价于“纯神经所有历史”，也不等价于某个日期以后。旧版本没有显式split_seed时会随噪声seed重新划分通道，已有记录足以证明后续三个窗口大部分信道被历史评测覆盖。

固定test中只有617个通道未出现在所有已记录ID内；这不证明它们从未被评测，因为还有24个缺失ID归档以及可能缺失的日志。工具因此无条件输出blindness_certified=false，不能仅因交集0自动发放盲测证明。

## 尚未解决的证据边界

24个缺少ID的归档包括早期amplitude/analytic/identity/phase诊断、prefix_scores，以及V192/V194/V204/V210的prefix缓存。完整路径在快照中。仅根据文件名中的seed推断通道不足以证明实际运行参数，需要执行命令、生成器版本及对应索引证据；本次不进行无证据补填。父模型梯度训练的完整谱系也不是这个索引扫描工具能够证明的，当前两条纯神经训练入口代码固定split1176只能证明其自身切分规则。

确认要求应区分：

- 与当前梯度训练和validation无交叉：上述三个窗口在固定split1176规则下成立，但仍需核对候选完整训练谱系。
- 与本轮offset2000选型不重叠：成立，且未为本次审计运行任何模型评分。
- 在整个项目历史里从未评测：上述窗口明确不成立；617个未记录通道也尚未获证。
- 线上达到69：本地任何检查均不能代替实际提交成绩。

## 后续执行约束

### 2026-09-09 10:47：V227父模型谱系的首个缺口

只读检查现存artifacts/pure_neural_v227/joint_low/training_report.json，以及同级calibration.json/latest_training.json：初始化字段仅为`baseline replicated into 2 experts`，没有baseline-dir、父权重哈希或当时训练器哈希。文档pure-neural-joint-snr-v227.md声明父模型为V224，且Encoder/Tx1/Rx1冻结，但文档声明本身不是完整执行来源证明。

Git提交ffaa902中对应训练器确实固定`deterministic_split_indices(len(data), seed=1176)`，args.seed仅控制抽样与随机流。这能证明该版本代码的切分方式，不能单凭现存代码证明当时实际运行的源码/父权重。V224的soft_score_long及soft_score_t05_bw025目录中也没有各自的training_report.json，不能套用新版逐目录报告约定臆造记录。

后续应找到V224实际执行命令、历史源版本和不可变父权重，再逐级追溯；可用冻结分支权重逐张量对应补充来源证据，但相同张量只证明相应组件，不证明其余训练历史。当前没有判定已经发生梯度泄漏，也没有认证完整谱系无泄漏；这个缺口不影响现有V230以后单因素实验共享父模型的事实，但限制了独立确认的表述。本次没有读取新窗口分数、运行模型推理或修改在训文件。

### 2026-09-09 12:22：冻结组件逐张量来源核查

只在CPU读取现存权重（torch.load、weights_only=True），对V227 joint_low中的Encoder专家0、Tx专家1、Rx专家1，移除`experts.N.`键名前缀后，与V224三个目录的单专家核心逐键、逐形状、torch.equal精确比较；没有运行模型或读取新窗口分数。结果如下，分母是该组件全部state_dict张量数量，不是抽样检查：

| V224候选目录 | Encoder相同张量 | Tx1相同张量 | Rx1相同张量 |
|---|---:|---:|---:|
| soft_score_long | 4/4 | 86/86 | 76/76 |
| soft_score_t05_bw025 | 0/4 | 0/86 | 0/76 |
| soft_score_fair50 | 0/4 | 0/86 | 0/76 |

全部比较键集合与形状一致；只有soft_score_long三组件完全匹配。它的三个文件SHA256分别为：

- encoder.pth：`d6431d8146c5d62718dba9131ea703ba24cd9e9ca98d545d7bd394b0de6d81bb`，与V227文件字节也相同。
- transmitter.pth：`a0929b1d8b2fe3d7c52eff2811259a7bfb0184164214b5acbee6eeabf648960b`。
- receiver.pth：`9ce7180213b1a97197884837168d977094885cf09ba29fc8866fa2e5fdca5b3e`。

被比较V227 Tx/Rx完整双专家文件SHA256为`09f5a29263b69aa556f7e02638c004554384bcd7ddb4e095cbc094aaffb79cf5`、`79702014a8b9176ec1660ef7aba5fae28a3be876020095f3515e10438c1b41c0`。Tx/Rx的文件哈希不同是因为V227有双专家；上述精确相同比较的是冻结高SNR专家核心，而非整个文件。

这支持文档所述V224-long冻结组件来源，排除了另两个现存候选是这些冻结张量的直接相同副本；不证明V227已训练低专家的初始化，也不证明V224-long实际执行命令、数据切分或更早祖先来源。特别是artifacts/pure_neural_v224/calibration.json现存内容为seed15230、fairness0.5、带宽0.01，对应另一实验，**不能当作soft_score_long的逐目录报告**。V224-long缺少独立training_report的证据缺口仍在，不把组件匹配升级成完整谱系认证，不修改历史报告补填来源。

训练继续正常进行，不因这次口径审计中断。候选达到69附近时先冻结四文件哈希和检查点，先核对谱系与剩余缺失记录，再登记确认窗口、噪声和比较规则，只运行冻结候选及冻结对照。不能看完各窗口分数后选最好窗口；后续若据确认结果再次优化，该窗口也变成开发证据，不能反复称独立确认。

可把固定后续窗口作为“未用于本轮offset2000选型、但与历史评测有交叉的确认”，明确限制；不能把它包装为完全未见盲测。若要严格覆盖全项目从未选型的要求，须补足617个潜在未见通道及缺失记录证据，或使用确实独立的新数据/实际线上评测。当前没有因此认定达标，目标仍未完成。
