# V228：真实硬判决排名 + 平滑替代梯度（预登记）

## 单因素对照

两组均从同一 V224 权重初始化，使用原单模型、B1152/k8、全部模块训练。seed=15234、batch=100、Adam lr=5e-6、6000 步、每750步验证、2000验证通道、patience=8。T=0.5、带宽0.025、BCE权重0.05、公平权重0.3、分位0.1不变。

- control：原 soft_score，按软正确率排名。
- treatment：hard_rank_score，按真实逐UE硬判决正确率排名。

两组初始权重、样本序列、噪声、验证集、预算相同，唯一训练目标区别是排名/前向分数定义。固定验证选择各自最佳 checkpoint，并报告同一步结果，避免只挑各自峰值解释。

## 目标的定义与限制

前向 bit 正确率使用官方 `(logit>=0)==bit`；反向借用 sigmoid(signed_logit/0.5) 梯度（straight-through estimator）。UE 真实正确率排序后，前向取精确线性插值 P10，反向保留 P10 附近高斯排名加权梯度。加0.05 BCE保持与对照一致。

这样，去掉 BCE 后的前向损失值精确等于该 batch 的负评分，但反向是**有偏替代梯度**，不是不可导硬评分的数学导数；不能保证真实分数单调升高。保留平滑尾部梯度是为了避免只更新两条位于精确分位点的样本。

## 验证

单元测试涵盖 logit=0、target=0/1、短输出0.5补偿、前向与官方公式一致、梯度有限且方向正确、软/硬排名不同时梯度确有变化。候选仅通过固定留出集新协议配对复评后才考虑晋级。

## 完整单因素结果

两组6000步均完成、最佳均为step5250，起点均67.852727；模型、样本和噪声序列一致。

| 项目 | soft_score对照 | hard_rank_score |
|---|---:|---:|
| 固定验证best5250 | 67.897599 | 67.893348 |
| 固定验证last6000 | 67.875626 | 67.859562 |
| 留出noise22701 | 67.921233 | 67.897819 |
| 留出noise22702 | 68.159933 | 68.185391 |
| 留出noise22703 | 68.278225 | 68.298813 |
| 留出均值 | **68.119797** | **68.127341** |

硬排名相对同预算对照仅+0.007544，通道配对bootstrap95%区间[-0.016267,+0.028037]跨零；三个噪声种子差值为-0.023413/+0.025458/+0.020588。不能确认额外收益，不凭这一结果单独晋级。

相对未续训V224的+0.057407中，普通软损失续训已贡献约+0.049863；不能把总提升全部归功于损失改动。中低SNR部分分箱略好，而高SNR部分略差；两组P10差值存在离散跳档。

完整训练历史：`benchmarks/pure_neural_hard_rank_v228_training.json`、`benchmarks/pure_neural_soft_control_v228_training.json`。完整逐区间、P10及配对区间：`benchmarks/pure_neural_hard_vs_soft_v228.json`。运行耗时受并行评测和另一组训练影响，不作为这两个损失的速度消融。

## 组合特性的边界

损失是训练规则，不能像推理层一样通过直接拼接权重“合并”。若本消融证明有益，必须在V227联合SNR专家上重新应用该损失训练，再做交叉对照。

V227的低模式Tx/Rx与V224共享Encoder配套；若将V228全模块续训后的Encoder直接替换进去，会改变低模式的反馈分布。不能未经验证就把各版本单独最好的模块拼成“冠军”。
