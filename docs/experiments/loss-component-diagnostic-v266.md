# V266：现有评分损失与 BCE 梯度分解（训练前登记）

V265完整消融未改善低SNR用户，不再沿梯度冲突推断扩大解除共享。V245曾改变未RMS归一化的soft-score BCE权重，其结论不能直接迁移到当前RMS目标。先只检查现有两个损失项的真实参数梯度关系，不搜索权重、不训练新模型。

固定V257已发布源检查点，split1176最前400个训练信道，seed26610，4批各100信道，原独立均匀SNR、全1152位、eval模式。冻结Encoder，检查全部72,744,448个唯一Tx/Rx参数。分别计算原评分代理项、0.05加权BCE项及原总损失的autograd梯度，验证线性分解，按Tx/Rx/总体输出范数及余弦。不做优化器更新，不保存权重，不读取测试切片分数作选择。

必须先提交推送代码、协议和测试，再执行。输出采用排他创建，绑定模型、数据和源码指纹；结束复核指纹及无累积梯度。不重跑覆盖失败或已完成记录。显存上限40%。

四批局部欧氏梯度只是描述，不能代表Adam实际更新，也不能据此承诺减小BCE会提分。若进一步训练，需另立单因素协议及完整预算；本次不是新评分、确认集或晋级证据。
