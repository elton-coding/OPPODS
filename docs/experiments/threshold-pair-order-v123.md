# V123 阈值码字对内顺序扫描

日期：2026-08-24  
分支：`feature/threshold-pair-order-v123`  
对照：V123

保持 start65 Walsh 行集合与门限映射不变，测试两种最小顺序变换：

- `swap_pairs`：每个 base/complement 对内交换顺序；
- `flip_last`：只翻转第 31 个未成对模板；
- 两者同时启用。

seed1176/1177/1178、300 样本结果表明没有候选三 seed 全正：

- `flip_last` 约为 `+0.000810/-0.000810/+0.000810`，纯离散抖动；
- `swap_pairs` 在 seed1176/1177 退化，但 seed1178 提升，方向冲突；
- 同时启用同样方向冲突。

不进入大样本复验，V123 码字顺序保持不变。两个开关仅保留在特性分支。
